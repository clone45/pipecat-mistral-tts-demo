[project]
name = "pipecat-mistral-tts"
version = "0.1.0"
description = "Pipecat TTS adapter for Mistral's Voxtral text-to-speech API"
requires-python = ">=3.11"
dependencies = [
    "pipecat-ai[daily,silero]==0.0.108",
    "aiohttp",
    "numpy",
    "python-dotenv",
    "loguru",
]
